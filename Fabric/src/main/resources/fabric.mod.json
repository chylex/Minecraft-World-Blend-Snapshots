{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "description": "${description}",
  "version": "${version}",
  "license": "${license}",
  
  "authors": [
    "${author}"
  ],
  
  "contact": {
    "issues": "${issuesURL}",
    "sources": "${sourcesURL}"
  },
  
  "mixins": [{
    "config": "${id}.mixins.json"
  }],
  
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=1.18.1"
  }
}
